{
  "version": 2,
  "builds": [
    {
      "src": "api/*.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["sim_kalkulator/**", "przyklady/**", "web/**"]
      }
    }
  ],
  "routes": [
    { "src": "/api/([^/]+)/?", "dest": "/api/$1.py" },
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
